«адача тестирование пам€ти.

»спользую JDK 1.7, maven 3.0

«апуск: выбрать пакет src/test/java, запустить как junit тест

ѕод каждую подзадачу написан отдельный тест

1) задание на сравнение потоков и процессов(ThreadTest):

»тог: потоки медленнее. ѕричина - потоки дел€т общую пам€ть, а процессам нет.


2) задание про страницы и подкачку(PageTest):

»тог: произвольна€ запись работает медленнее, чем последовательна€, потому что приходитс€ посто€нно подгружать новые страницы.


3) задание про ложное разделение(SeparationTest):

»тог: счетчики, который расположены далеко друг от друга, работают быстрее, потому что €чейки пам€ти в таком случае попадают в разные строки кеша.


4) задание про матрицы(MatrixTest):
	(по приколу упоролс€ с generic-ами)

»тог: умножение матриц, хранимых блоками работает быстрее. 
ѕотому что при таком варианте работы увеличиваетс€ веро€тность того что проход по столбцу не будет требовать разных строк кеша

ѕримечание: 
	можно попробовать помен€ть в pom-е версию JDK, и посмотреть как изменитс€ производительность 
	(сам поиграл - вначале показалось что 7-а€ помедленней 5 и 6, но потом закономерность пропала...)